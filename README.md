# cmdloader


A (almost) undetectable loader for any file



# Example Usage
Everything (Path):
'"load.cmd path --silent"'

Everthing (online):
'"load.cmd url --silent"'

⚠️ Not silent (not recommended):
'"load.cmd path"'
